# 🎯 DEPLOY NHANH - 3 BƯỚC

## 📦 BƯỚC 1: PUSH LÊN GITHUB (2 phút)

1. Tạo repo mới: https://github.com/new

   - Name: `quiz-fun`
   - Public
   - Không tick gì cả
   - Create repository

2. Chạy trong PowerShell:

```powershell
cd C:\GameDemo

# Thay YOUR_USERNAME bằng username GitHub của bạn
git remote add origin https://github.com/YOUR_USERNAME/quiz-fun.git
git branch -M main
git push -u origin main
```

✅ Done! Code đã lên GitHub.

---

## 🔧 BƯỚC 2: DEPLOY BACKEND (5 phút)

1. Mở: https://render.com
2. Sign up với GitHub
3. **New +** → **Web Service**
4. Connect GitHub → Chọn repo `quiz-fun`
5. Điền:
   ```
   Name:          quiz-backend
   Region:        Singapore
   Root Dir:      backend
   Build:         npm install
   Start:         npm start
   Instance:      Free
   ```
6. **Create Web Service**
7. Đợi 5 phút → Copy URL backend

**Backend URL của bạn:**

```
https://quiz-backend-xxxx.onrender.com
```

✅ Test: Mở `https://quiz-backend-xxxx.onrender.com/api/questions`

---

## 🎨 BƯỚC 3: DEPLOY FRONTEND (3 phút)

1. Mở: https://vercel.com/signup
2. Sign up với GitHub
3. **Add New** → **Project**
4. Chọn repo `quiz-fun`
5. Điền:
   ```
   Framework:     Vite
   Root Dir:      frontend (click Edit)
   Build:         npm run build
   Output:        dist
   ```
6. **Environment Variables** → Add:

   ```
   Name:   VITE_API_URL
   Value:  https://quiz-backend-xxxx.onrender.com/api
   ```

   ⚠️ **Thay URL backend của bạn + thêm `/api` ở cuối!**

7. **Deploy**
8. Đợi 2 phút → **Visit** để xem web!

**Frontend URL của bạn:**

```
https://quiz-fun-xxxx.vercel.app
```

---

## ✅ XONG!

Bạn đã có web hoạt động 24/7:

🔗 **Frontend:** https://quiz-fun-xxxx.vercel.app  
🔗 **Backend:** https://quiz-backend-xxxx.onrender.com

### Test thử:

1. Vào web
2. Click "Chơi ngay"
3. Làm quiz
4. Vào "Admin" thêm câu hỏi

### Update code:

```powershell
cd C:\GameDemo
# Sửa code...
git add .
git commit -m "Update"
git push
```

→ Tự động deploy lại!

---

## 🆘 Lỗi?

**Backend không chạy:**

- Đợi thêm 2-3 phút
- Xem Logs trong Render

**Frontend lỗi kết nối:**

- Check `VITE_API_URL` trong Vercel Settings
- Phải có `/api` ở cuối
- Redeploy

**Trang reload bị 404:**

- File `vercel.json` đã có sẵn
- Push lại code

---

📚 **Chi tiết:** Xem `DEPLOY-STEP-BY-STEP.md`
