# 🚀 Quick Start Guide

Hướng dẫn nhanh để chạy ứng dụng Quiz Fun trên máy local.

## ⚡ Bắt đầu nhanh

### Bước 1: Cài đặt Backend

Mở PowerShell và chạy:

```powershell
cd C:\GameDemo\backend
npm install
npm start
```

✅ Backend sẽ chạy tại: **http://localhost:4000**

### Bước 2: Cài đặt Frontend

Mở PowerShell window mới và chạy:

```powershell
cd C:\GameDemo\frontend
npm install
npm run dev
```

✅ Frontend sẽ chạy tại: **http://localhost:5173**

### Bước 3: Mở trình duyệt

Truy cập: **http://localhost:5173**

---

## 🎮 Sử dụng

### Trang chủ
- Click "Chơi ngay!" để bắt đầu quiz
- Click "Quản lý" để vào trang admin

### Chơi game
1. Chọn số câu hỏi (1-20)
2. Chọn thời gian (30-300 giây)
3. Click "Bắt đầu chơi!"
4. Trả lời câu hỏi
5. Click "Nộp bài" khi hoàn thành
6. Xem kết quả và chi tiết

### Quản lý câu hỏi (Admin)
- **Thêm câu hỏi:** Click "Thêm câu hỏi"
- **Sửa:** Click nút "Sửa" ở mỗi câu hỏi
- **Xóa:** Click nút "Xóa" (có xác nhận)
- **Import CSV:** Click "Nhập CSV" → chọn file
- **Export CSV:** Click "Xuất CSV"

---

## 📤 Import/Export CSV

### Format CSV chuẩn:

```csv
question,choice1,choice2,choice3,choice4,correctIndex
"2 + 2 = ?","3","4","5","6",1
"Con mèo kêu gì?","Gâu gâu","Meo meo","Ò ó o","Cục tác",1
```

**Lưu ý:**
- File CSV phải có encoding **UTF-8 with BOM**
- `correctIndex` bắt đầu từ 0 (0=choice1, 1=choice2, ...)
- Có file mẫu: `sample-questions.csv`

### Cách tạo CSV trong Excel:
1. Tạo bảng với các cột như trên
2. File → Save As → CSV UTF-8 (Comma delimited)

---

## 🛠️ Troubleshooting

### Backend không chạy được

**Lỗi:** `Cannot find module 'express'`
```powershell
cd backend
npm install
```

**Lỗi:** `Error: EADDRINUSE: address already in use :::4000`
```powershell
# Tìm process đang dùng port 4000
netstat -ano | findstr :4000
# Kill process (thay PID bằng số từ lệnh trên)
taskkill /PID <PID> /F
```

### Frontend không kết nối được backend

**Kiểm tra:**
1. Backend đã chạy chưa? (http://localhost:4000/api/questions)
2. Check browser Console (F12) xem có lỗi CORS không

**Fix:**
```powershell
# Khởi động lại backend
cd backend
npm start
```

### CSV import lỗi

**Kiểm tra:**
- File có đúng format không?
- Encoding phải là UTF-8
- Có header row không?

---

## 📁 Cấu trúc Database

SQLite database tự động tạo tại: `backend/db.sqlite`

**Schema:**
```sql
CREATE TABLE questions (
  id INTEGER PRIMARY KEY,
  text TEXT NOT NULL,
  choice1 TEXT NOT NULL,
  choice2 TEXT NOT NULL,
  choice3 TEXT NOT NULL,
  choice4 TEXT NOT NULL,
  correctIndex INTEGER NOT NULL,
  createdAt INTEGER
)
```

**Xem database:**
- Download [DB Browser for SQLite](https://sqlitebrowser.org/)
- Mở file `backend/db.sqlite`

---

## 🎨 Tùy chỉnh

### Thay đổi màu chủ đạo

File: `frontend/src/main.jsx`
```javascript
colorPrimary: '#ff6b9d', // Đổi màu này
```

### Thay đổi font chữ

File: `frontend/index.html`
```html
<!-- Đổi font Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Comic+Neue:wght@400;700&display=swap" rel="stylesheet">
```

File: `frontend/src/styles.css`
```css
font-family: 'Comic Neue', sans-serif;
```

### Thêm âm thanh

1. Thêm file MP3 vào `frontend/public/sounds/`
2. Trong `Play.jsx`:
```javascript
const correctSound = new Audio('/sounds/correct.mp3')
correctSound.play()
```

---

## 📊 Các lệnh hữu ích

### Backend
```powershell
npm start          # Chạy server
npm run dev        # Chạy với nodemon (auto-reload)
```

### Frontend
```powershell
npm run dev        # Development mode
npm run build      # Build production
npm run preview    # Preview production build
```

---

## 🔐 Bảo mật Admin (Tùy chọn)

Để bảo vệ trang Admin:

1. Thêm vào `backend/index.js`:
```javascript
const ADMIN_PASSWORD = 'matkhaucuaban'

app.use('/api/questions', (req, res, next) => {
  const auth = req.headers.authorization
  if (auth !== `Bearer ${ADMIN_PASSWORD}`) {
    return res.status(401).json({ error: 'Unauthorized' })
  }
  next()
})
```

2. Trong `frontend/src/api.js`:
```javascript
api.defaults.headers.common['Authorization'] = 'Bearer matkhaucuaban'
```

---

## 📞 Hỗ trợ

### Tài liệu đầy đủ:
- `README.md` - Tổng quan
- `DEPLOYMENT.md` - Hướng dẫn deploy
- `backend/README.md` - API docs
- `frontend/README.md` - Frontend docs

### API Testing:
```powershell
# Test backend API
curl http://localhost:4000/api/questions
```

---

## ✨ Tips

1. **Development workflow:**
   - Chạy cả backend và frontend cùng lúc
   - Dùng 2 terminal windows

2. **Backup database:**
   ```powershell
   copy backend\db.sqlite backup\db.sqlite.backup
   ```

3. **Reset database:**
   - Xóa file `backend/db.sqlite`
   - Restart backend → tự động tạo lại với dữ liệu mẫu

---

Made with ❤️ - Happy Teaching! 🎈
